# Explore-Expansion Follow-ups — Tasks

Backlog only — nothing in progress. Confirm direction with the user, then
`/new-feature` (or convert the chosen item into its own active task) before building.

## Deferred from explore-expansion (small)
- [ ] Per-game stickers for the 5 new games (`clock-master`, `body-explorer`,
      `star-gazer`) in `data/stickers.js` + `sticker.*` i18n ×6 (+ adjust
      `tests/stickers.test.js` if it enumerates ids)
- [ ] Translate educational fact text (`data/body.js`, `data/space.js`) + new
      story prose, if a non-EN locale is prioritised

## Deepening candidates (pick the area that lands well)
- [ ] Clock: "quarter to" phrasing / word-clock reading / digital→analog variant
- [ ] Human Body: 3rd game (label body parts / senses); richer layer art
- [ ] Space: more constellations; planet quiz; moon phases
- [ ] Library: real illustrations for the 20 new stories (WebP pipeline exists);
      or "read to me" auto-advance
- [ ] More recipes / more 2-player games

## Housekeeping
- [ ] (when a direction is chosen) archive this backlog folder or fold the chosen
      item into a proper `dev/active/<name>/` task
